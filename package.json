{
  "name": "angular-learning",
  "version": "1.0.0",
  "private": true,
  "description": "Shared tooling for the Angular learning projects",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "prettier": "^3.9.6"
  }
}
